
## 1) المتطلبات قبل التثبيت

- **Node.js** (نسخة 20 أو أحدث) — https://nodejs.org
- **Python 3.13 أو أحدث** — https://python.org
- بعد فك ضغط الملف، افتح طرفية (Terminal / PowerShell) جوا مجلد المشروع.

## 2) تثبيت الفرونت (Next.js)

جوا مجلد المشروع الرئيسي:

```
npm install
npx prisma generate
```

قاعدة البيانات صارت **PostgreSQL مستضافة** (مو ملف محلي)، فما في شي تنزّله — بس لازم يكون `DATABASE_URL` معرّف بملف `.env` (شوف القسم الجاي). القاعدة معمّرة أصلاً بالبيانات الحقيقية: 400 دواء و2182 تفاعل.

إذا احتجت تعمّر قاعدة جديدة من الصفر:

```
npx prisma migrate deploy
npx prisma db seed
```

> ملاحظة لشبكات معيّنة: بعض مزوّدي الإنترنت بيحجبوا المنفذ 5432. `prisma migrate` بيستخدمه دايماً، فلو فشل بخطأ P1001، شغّل الـmigration من GitHub Actions (workflow **تجهيز قاعدة البيانات** بالريبو). التطبيق نفسه والـseed ما بيتأثروا — بيتصلوا عبر HTTPS.

## 3) تثبيت الباك إند (FastAPI)

```
cd api
python -m venv .venv
```

تفعيل البيئة الافتراضية:
- **Windows (PowerShell):** `.venv\Scripts\Activate.ps1`


بعد التفعيل:

```
pip install -r requirements.txt
```

## 4) ملف `.env`

لازم يكون موجود بجذر المشروع (نفس مكان `package.json`) وفيه:

```
DATABASE_URL="postgres://...@pooled.db.prisma.io:5432/postgres?sslmode=require"
FASTAPI_URL="http://localhost:8000"
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="rxchef2026"
ADMIN_SESSION_SECRET="3695f654aaf71dd8f54f96f8c8f23558577ba5550b3a17aba7ca55148070c38b"
```

`DATABASE_URL` الحقيقي موجود بملف `.env` المرفق (مو مرفوع على GitHub). ملف `.env.example` بجذر المشروع فيه شرح كل مفتاح.

إذا الملف مو موجود بالضغطة (بعض أدوات الضغط بتخفي الملفات يلي تبدأ بنقطة)، أنشئه يدويًا بنفس القيم فوق.

## 5) التشغيل — طرفيتين بنفس الوقت

**الطرفية الأولى — الباك إند** (جوا مجلد `api`، بعد تفعيل `.venv`):

```
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

تأكيد إنه شغّال: افتح http://localhost:8000/health لازم يطلع `{"status":"ok"}`

**الطرفية الثانية — الفرونت** (جوا مجلد المشروع الرئيسي):

```
npm run dev
```

افتح المتصفح على: **http://localhost:3000**

## 6) حساب تجريبي — لوحة الإدارة

**الرابط:** http://localhost:3000/admin/login

- المستخدم: `admin`
- كلمة السر: `rxchef2026`

من اللوحة فيك تدير: قاعدة الأدوية، سجل العمليات، الرسائل، آراء المجرّبين.



المفروض يطلعلك تفاعل "شديد" حقيقي مع بدائل مقترحة. إذا طلعت رسالة خطأ، الأغلب الباك إند (بورت 8000) مو شغّال.

## 8) جدول تجربة شامل لكل الحالات

كل صف بالجدول جرّبناه فعليًا عبر الـAPI مباشرة وتأكدنا من نتيجته. جرّبهم أنت كمان من الواجهة وعبّي عمودَي "تجريب" و"النتيجة الفعلية" حتى تتأكد إنه كل شي شغّال صح عندك.

| الحالة | أدوية تكتبها | النتيجة المتوقعة | تجريب | النتيجة الفعلية |
|---|---|---|---|---|
| تطابق تام، بلا تفاعل | `Metformin` + `Lisinopril` | مطابقة 100%، صفر تفاعل | ☐ | |
| خطأ إملائي (fuzzy) | `Aspirn` + `Metfformin` | تصحيح تلقائي 92%/95% | ☐ | |
| دواء غير موجود | `Vitacomplex Forte` | يترفض، يقترح أقرب اسم | ☐ | |
| تفاعل شديد + بدائل | `Aspirin` + `Coumadin` | شديد، ثقة 99%، بديلين | ☐ | |
| تفاعل شديد + بدائل تانية | `Atorvastatin` + `Clarithromycin` | شديد، ثقة 70%، بديلين (Livalo, Lescol) | ☐ | |
| حالة خلاف كتالوج/نموذج | `Lisinopril` + `Spironolactone` | الكتالوج يقول "متوسط"، بس النموذج تنبأ "شديد" بثقة 79% — فرصة توضّح إنه النموذج تصنيف مستقل مو نسخة كربونية عن الكتالوج | ☐ | |
