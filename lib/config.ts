/** إعدادات الخدمات الخارجية — مقروءة من البيئة، بلا أي رابط مكتوب بالكود.
 *  تغيير الاستضافة = تعديل متغيّر بيئة واحد، بلا لمس أي ملف. */

/** خدمة التحليل (FastAPI): الخوارزميات الأربع. القيمة الافتراضية للتطوير
 *  المحلي فقط — بالإنتاج لازم FASTAPI_URL يكون معرّف بإعدادات الاستضافة. */
export const FASTAPI_URL = (process.env.FASTAPI_URL ?? "http://localhost:8000").replace(/\/+$/, "");
